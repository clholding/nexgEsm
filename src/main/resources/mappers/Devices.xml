<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.nexg.esm.mapper.DevicesMapper">

	<select id="getDeviceGroupByLogin" parameterType="kr.nexg.esm.dto.DevicesVo" resultType="map">
		/* DevicesMapper.getDeviceGroupByLogin */	
		CALL sesm.getDeviceGroupByLogin( #{id}, #{mode} );
	</select>
	<select id="getDeviceListByLogin" parameterType="kr.nexg.esm.dto.DevicesVo" resultType="map">
		/* DevicesMapper.getDeviceListByLogin */	
		CALL sesm.getDeviceListByLogin( #{id}, '2', #{mode} );
	</select>
	<select id="getDeviceGroup" resultType="map">
		/* DevicesMapper.getDeviceGroup */	
		CALL sesm.getDeviceGroup();
	</select>
	<select id="getDeviceList" resultType="map">
		/* DevicesMapper.getDeviceList */	
		CALL sesm.getDeviceList(2);
	</select>
	
	
	<select id="getDeviceInfoList" parameterType="kr.nexg.esm.dto.DevicesVo" resultType="map">
		/* DevicesMapper.getDeviceInfoList */	
		CALL sesm.getDeviceInfoList( #{deviceIDs} );
	</select>
	
	<select id="getProductList" parameterType="kr.nexg.esm.dto.DevicesVo" resultType="map">
		/* DevicesMapper.getProductList */	
		CALL sesm.getProductList( #{type} );
	</select>

</mapper>